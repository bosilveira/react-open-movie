import { Title  } from '@ui5/webcomponents-react';
import './styles.scss';

function Header() {
  return (
    <header>
        <Title level='H2'>
            The Open Movie Database
        </Title>
    </header>
  );
}

export default Header;
